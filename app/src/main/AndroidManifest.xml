<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.libyaproject">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/police"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">


        <activity
            android:name=".Activitys.LoginActivity"
            android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activitys.MainActivity"
            android:label="الصفحة الرئسية" />
        <activity
            android:name=".Activitys.CarStolenActivity"
            android:label="سيارة المسروقة" />
        <activity
            android:name=".Activitys.WantedActivity"
            android:label="المطلوب" />
        <activity
            android:name=".Activitys.ChangePasswordActivity"
            android:label="تغير كلمة المرور"/>
        <activity
            android:name=".Activitys.ChangePhoneNumberActivity"
            android:label="تغير رقم الهاتف"/>
        <activity android:name=".Activitys.SmsCodeActivity" />
        <activity android:name=".Activitys.ForgetPasswordActivity" />
    </application>

</manifest>